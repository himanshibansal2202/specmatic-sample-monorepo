version: 3

dependencies:
  services:
    - service:
        $ref: "#/components/services/orderBff"
        runOptions:
          $ref: "#/components/runOptions/orderBffMock"

specmatic:
  governance:
    report:
      formats:
        - html
        - ctrf
      outputDirectory: "{SPECMATIC_REPORT_DIR:build/reports/specmatic}"
  settings:
    test:
      schemaResiliencyTests: all
      timeoutInMilliseconds: 6000

components:
  sources:
    specmaticOrderContracts:
      git:
        url: https://github.com/specmatic/specmatic-order-contracts.git
  services:
    orderBff:
      description: Order BFF consumed by the React frontend
      definitions:
        - definition:
            source:
              $ref: "#/components/sources/specmaticOrderContracts"
            specs:
              - io/specmatic/examples/store/openapi/product_search_bff_v6.yaml
  runOptions:
    orderBffMock:
      openapi:
        type: mock
        baseUrl: "{STUB_BASE_URL:http://localhost:8090}"
